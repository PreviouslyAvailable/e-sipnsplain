# Sip'n'Splain — slide index

Canonical 1-based map. Prefer **`#id`** in chat; numbers drift if slides are inserted.

Say: `slide 1` or `#win95` — both resolve via `Sipnsplain.go` / this table.

**Overlays (not numbered):**
- **Boot** (`boot-bsod`) — cold open: funny load lines + dial-up → click/key (after all lines) → `#win95`.

| # | id | label | theme / era |
|---:|---|---|---|
| 1 | `win95` | Win95 desktop hub | Win95 scrapbook (`1995`) |
| 2 | `win98` | Windows 98 hub | Windows 98 hub (`win98`) |
| 3 | `continue` | Continue… | Prologue (`prologue`) |
| 4 | `age-8` | GBA SP · 2003 | GBA SP · 2003 (`2003`) |
| 5 | `age-11` | Bebo · 2006 | Bebo · 2006 (`2006`) |
| 6 | `age-14` | Minecraft · craft web | Early craft web (`2009`) |
| 7 | `make-impossible` | Make something impossible | AI-native finale (`2025`) |
| 8 | `source-reveal` | Source reveal | AI-native finale (`2025`) |
